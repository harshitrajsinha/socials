apiVersion: v1
kind: Service
metadata:
  name: redis-svc
  namespace: redis
spec:
  selector:
    app.kubernetes.io/name: redis-app
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
  type: ClusterIP

